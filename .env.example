# ============================================
# FAM Backend Environment Configuration
# ============================================
# This file contains SENSITIVE and ENVIRONMENT-SPECIFIC settings.
# Non-sensitive settings (pagination, file upload limits, etc.) are in appsettings.json
#
# Copy this file to .env and update with your values.
# DO NOT commit .env file to git!
# ============================================

# ------- Environment -------
ASPNETCORE_ENVIRONMENT=Development

# ------- Database Configuration (Required) -------
# Provider: PostgreSQL or MongoDB
DB_PROVIDER=PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fam_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# ------- JWT Authentication (Required) -------
# Keys are managed in database via SigningKey table
# Access JWKS at: /.well-known/jwks.json or /api/auth/jwks
JWT_ISSUER=FAM.API
JWT_AUDIENCE=FAM.Client
ACCESS_TOKEN_EXPIRATION_MINUTES=60
REFRESH_TOKEN_EXPIRATION_DAYS=30
MAX_FAILED_LOGIN_ATTEMPTS=5
ACCOUNT_LOCKOUT_MINUTES=15

# ------- MinIO Storage (Required - Sensitive) -------
MINIO_HOST=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=your_password_here
MINIO_USE_SSL=false
MINIO_BUCKET_NAME=fam-assets
MINIO_CONSOLE_PORT=9001

# ------- Seq Logging -------
SEQ_PORT=5341
SEQ_UI_PORT=9002
SEQ_ADMIN_PASSWORD=Admin@123

# ------- Redis Configuration (Required - Sensitive) -------
# For OTP caching and distributed cache
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_INSTANCE_NAME=fam:

# ------- Email Configuration (Required - Sensitive) -------
# Provider: "Brevo" (recommended) or "Smtp"
# Emails are queued and sent asynchronously via background service
EMAIL_PROVIDER=Smtp
EMAIL_FROM=noreply@fam.com
EMAIL_FROM_NAME=FAM System

# Brevo (formerly Sendinblue) - Recommended for production
# Get API key from: https://app.brevo.com/settings/keys/api
BREVO_API_KEY=

# SMTP Fallback (for development or legacy systems)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_ENABLE_SSL=true

# ------- Application URLs -------
# Backend API base URL (for callbacks, webhooks, public file URLs, etc.)
# Development: http://localhost:8000 (with port)
# Staging: https://api-staging.fam.yourdomain.com (no port, uses 443)
# Production: https://api.fam.yourdomain.com (no port, uses 443)
BACKEND_URL=http://localhost:8000

# Frontend base URL (for email links like reset password, verify email, etc.)
# Development: http://localhost:3000 (with port)
# Staging: https://staging.fam.yourdomain.com (no port, uses 443)
# Production: https://fam.yourdomain.com (no port, uses 443)
FRONTEND_URL=http://localhost
FRONTEND_PORT=3000
